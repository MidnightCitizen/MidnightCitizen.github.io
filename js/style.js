// Can also be included with a regular script tag


var options = {
  strings: ["",  "git push --force ^1000 <br> `pushed to origin with option force ^1000 `", "clear"],
  typeSpeed: 50,
  loop : true,
  showCursor: true
}

var typed = new Typed("#presentation", options);
